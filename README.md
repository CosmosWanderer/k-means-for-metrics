# k-means-for-metrics
A tool for clustering function calls by metrics.

## Setting up tool
### Linux
```bash
chmod +x setup.sh
./setup.sh
chmod +x run.sh
```

## Runnig tool
```bash
./run.sh metrics_example.csv
```